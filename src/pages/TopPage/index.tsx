import Layout from '../../Layout';

const TopPage = () => {
  return (
    <Layout user=''>
      <div
        style={{
          color: 'rgba(0,0,0,0.15)',
          height: '100%',
          fontSize: 300,
          position: 'fixed',
          wordBreak: 'break-all',
          fontWeight: 900,
          lineHeight: 0.7,
          fontFamily: 'Roboto, sans-serif',
        }}
      >
        under construction
      </div>
    </Layout>
  );
};

export default TopPage;
